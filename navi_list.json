---
layout: null
---
{
	"code" : 0 ,
	"data" : [
		 {% for post in site.posts %}
		{
			"title" : "{{ post.title }}",
			"keywords" : "{{ post.keywords }}",
			"path" : "{{ post.path }}",
			"description":"{{ post.description }}"
		}
		{% if forloop.rindex != 1  %}
		,
		{% endif %}
	   	{% endfor %}
	]
}
